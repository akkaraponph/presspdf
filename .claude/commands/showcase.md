Run the showcase example to generate all 15 feature demo PDFs, then verify they were created successfully.

```bash
go run ./examples/showcase
```

List the generated files and report any errors. If a demo fails, diagnose and fix the issue.
